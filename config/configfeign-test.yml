spring:
  application:
    name: feign
  thymeleaf:
    prefix: classpath:/static/
    suffix: .html
    cache: false

server:
  port: 8060
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true